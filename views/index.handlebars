<h1 style="text-align: center;">Eat-Da-Burgers!</h1>

<div class="container">
    <div class="row">
        <div class="col-6">
            <h2>Burger Waiting Area</h2>

            <ul>
                {{#each burgers}}
                    {{#unless devoured}}
                        <li>{{burger_name}} <button class="devour" data-id={{id}}>DEVOUR IT!</button></li>
                    {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="col-6">
            <h2>Devoured Burgers</h2>

            <ul>
                {{#each burgers}}
                    {{#if devoured}}
                        <li>{{burger_name}}</li>
                    {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>
</div>

<h2 style="text-align: center;">Order a Burger</h2>
<form class="create-form" style="text-align: center;">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="burger_name">
  </div>
  <button type="submit">Order Burger</button>
</form>
